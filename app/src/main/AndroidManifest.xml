<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.regardening_test">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ReGardeningtest">
        <activity android:name=".separate"></activity>
        <activity android:name=".styrofoam"></activity>
        <activity android:name=".iron" />
        <activity android:name=".glass" />
        <activity android:name=".plastic" />
        <activity android:name=".vinil" />
        <activity android:name=".paper" />
        <activity android:name=".collection" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>